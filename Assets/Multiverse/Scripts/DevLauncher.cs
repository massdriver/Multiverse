using UnityEngine;
using System.Collections;
using UnityEngine.Networking;

namespace Multiverse
{
    // To properly use NetworkManager
    //
    // Client load required by zone server scene (that's why ZoneMaster should send to client info about them already)
    // Spawn zone network manager
    // Connect
    public class DevLauncher : MonoBehaviour
    {
    }
}
